name: Deploy to Hosting

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout source code
      - name: Checkout
        uses: actions/checkout@v4

      # 2. Prepare SSH private key
      - name: Prepare SSH key
        run: |
          echo "${{ secrets.SSH_PRIVATE_KEY_B64 }}" | base64 -d > github_actions_key
          chmod 600 github_actions_key

      # 3. Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      # 4. Install frontend dependencies
      - name: Install npm dependencies
        run: npm ci

      # 5. Build frontend
      - name: Build frontend
        env:
          VITE_PUSHER_APP_KEY: ${{ secrets.VITE_PUSHER_APP_KEY }}
          VITE_PUSHER_APP_CLUSTER: ${{ secrets.VITE_PUSHER_APP_CLUSTER }}
        run: npm run build

      # 6. Prepare build directory
      - name: Prepare build
        run: |
          mkdir -p deploy_build
          cp -R public/build/. deploy_build/

      # 7. Prepare public static files
      - name: Prepare public assets
        run: |
          mkdir -p deploy_public
          cp -R public/. deploy_public/
          rm -f deploy_public/index.php deploy_public/.htaccess

      # 8. Upload frontend build
      - name: Upload build
        uses: appleboy/scp-action@v1
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          port: ${{ secrets.PORT }}
          key_path: github_actions_key
          source: "deploy_build/*"
          target: "/home/orderthe/deploy_build"
          strip_components: 1

      # 9. Upload public static files
      - name: Upload public assets
        uses: appleboy/scp-action@v1
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          port: ${{ secrets.PORT }}
          key_path: github_actions_key
          source: "deploy_public/*"
          target: "/home/orderthe/deploy_public"
          strip_components: 1

      # 10. Deploy Laravel
      - name: Deploy Laravel
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          port: ${{ secrets.PORT }}
          key_path: github_actions_key
          script: |
            set -e

            cd /home/orderthe/amazon

            echo "=== Update Laravel source ==="
            git fetch origin main
            git reset --hard origin/main

            echo "=== Install PHP dependencies ==="
            composer install --no-dev --optimize-autoloader

            echo "=== Update public static files ==="
            cp -R /home/orderthe/deploy_public/. /home/orderthe/public_html/

            echo "=== Link public storage ==="
            if [ -e /home/orderthe/public_html/storage ] && [ ! -L /home/orderthe/public_html/storage ]; then
              echo "ERROR: /home/orderthe/public_html/storage exists and is not a symlink"
              exit 1
            fi
            ln -sfn /home/orderthe/amazon/storage/app/public /home/orderthe/public_html/storage

            echo "=== Update frontend build ==="
            rm -rf /home/orderthe/amazon/public/build
            cp -R /home/orderthe/deploy_build /home/orderthe/amazon/public/build
            rm -rf /home/orderthe/public_html/build
            mv /home/orderthe/deploy_build /home/orderthe/public_html/build

            echo "=== Clear Laravel cache ==="
            php artisan optimize:clear

            echo "=== Deployment completed ==="